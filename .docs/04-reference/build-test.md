# ビルド＆テスト（Build & Test）

## セットアップ

プロジェクトに応じて設定してください。

```bash
# 依存関係インストール（例: uv, pip, poetry等）
# uv sync
# pip install -r requirements.txt
# poetry install

# 環境変数設定（プロジェクトに応じて）
# cp .env.example .env
```

## 実行

プロジェクトに応じて設定してください。

```bash
# テスト実行（例）
# pytest
# python -m pytest
# uv run pytest
```

## 前提条件

プロジェクトに応じて設定してください。

- **パッケージマネージャー**: uv, pip, poetry等
- **その他の依存関係**: プロジェクトに応じて設定

## 環境変数

プロジェクトに応じて設定してください。

以下の環境変数を`.env`ファイルに設定（プロジェクトに応じて変更）：

- `EXAMPLE_API_KEY`: 例（プロジェクトに応じて変更）

